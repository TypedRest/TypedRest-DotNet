mode: ContinuousDeployment

branches:
  # Mainline branches
  develop:
    label: alpha
    increment: Patch
  main:
    mode: ContinuousDelivery
    label: beta

  # Stabilization branches
  release:
    label: rc
  hotfix:
    label: rc
